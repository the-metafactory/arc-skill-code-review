# arc-manifest.yaml — capability declaration
schema: arc/v1
name: code-review
version: 0.2.1
type: skill
tier: custom

author:
  name: mellanon
  github: mellanon

license: MIT

provides:
  skill:
    - trigger: "review PR"
    - trigger: "code review"
    - trigger: "security audit"
    - trigger: "hardening review"
    - trigger: "skill review"

capabilities:
  bash:
    allowed: true
    restricted_to:
      - "gh pr *"
      - "gh api *"
